using UnityEngine;

public class LocalPlayerReferences : MonoBehaviour
{
    public GameObject handReference;
    public GameObject headReference; //Unused currently as we don't want player to see it's own hat but that may change
}
